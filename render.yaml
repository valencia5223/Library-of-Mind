services:
  - type: web
    name: baby-play-studio
    env: static
    buildCommand: cd baby-play-studio && npm install && npm run build
    staticPublishPath: ./baby-play-studio/dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
